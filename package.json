{
  "name": "linuxdo-enhanced",
  "version": "8.24.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "syntax": "node scripts/check-syntax.mjs",
    "check": "npm run syntax && npm run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-monkey": "^4.0.0"
  }
}
